{{! Кастомный api.mustache для openapi-generator (-g csharp). Пояснения ниже обычными комментариями //. Внутри этого блока не должно быть никаких mustache-скобок и парных фигурных закрывашек — иначе комментарий закроется раньше и хвост утечёт в вывод. }}
// <auto-generated/>
// Сгенерировано openapi-generator по кастомному шаблону. РУКАМИ НЕ РЕДАКТИРОВАТЬ —
// регенерация затрёт. Транспорт/ошибки — в ProjectApi.RestClient.
//
// Обёртка (доп-свойство wrapApi): все классы-обёртки складываются в один
// static partial class с именем из wrapperClass (partial'ы из разных файлов
// сливаются в один) → обращение <Wrapper>.FooApi. Имя движок выводит из версии
// в спеке: api/v1.0 → ApiV10, версии нет → Api. Включается на вкладке Config.
#nullable disable
using System;
using System.Threading;
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using ProjectApi;
using {{packageName}}.{{modelPackage}};
using {{packageName}}.Client;

namespace {{packageName}}.{{apiPackage}}
{
{{#wrapApi}}
    // Обёртки сгруппированы в один partial-класс на файл; partial'ы из разных
    // файлов сливаются в один {{wrapperClass}} → обращение {{wrapperClass}}.SomeApi.
    public static partial class {{wrapperClass}}
    {
{{/wrapApi}}
{{#operations}}
    /// <summary>Обёртки эндпоинтов «{{classname}}».</summary>
    public sealed class {{classname}}
    {
        private readonly RestClient _client;

        public {{classname}}(RestClient client) => _client = client;

{{#operation}}
        /// <summary>{{#summary}}{{summary}}{{/summary}}{{^summary}}{{httpMethod}} {{path}}{{/summary}}</summary>
{{#returnType}}
        public UniTask<{{{returnType}}}> {{operationId}}Async({{#allParams}}{{{dataType}}} {{paramName}}, {{/allParams}}CancellationToken ct = default)
        {
            var path = "{{{path}}}";
{{#pathParams}}
            path = path.Replace("{" + "{{baseName}}" + "}", System.Uri.EscapeDataString(Str({{paramName}})));
{{/pathParams}}
{{#hasQueryParams}}
            var __q = new List<string>();
{{#queryParams}}
            if ({{paramName}} != null) __q.Add("{{baseName}}=" + System.Uri.EscapeDataString(Str({{paramName}})));
{{/queryParams}}
            if (__q.Count > 0) path += "?" + string.Join("&", __q);
{{/hasQueryParams}}
            return _client.SendAsync<{{{returnType}}}>("{{httpMethod}}", path, {{#bodyParam}}body: {{paramName}}, {{/bodyParam}}ct: ct);
        }
{{/returnType}}
{{^returnType}}
        public UniTask {{operationId}}Async({{#allParams}}{{{dataType}}} {{paramName}}, {{/allParams}}CancellationToken ct = default)
        {
            var path = "{{{path}}}";
{{#pathParams}}
            path = path.Replace("{" + "{{baseName}}" + "}", System.Uri.EscapeDataString(Str({{paramName}})));
{{/pathParams}}
{{#hasQueryParams}}
            var __q = new List<string>();
{{#queryParams}}
            if ({{paramName}} != null) __q.Add("{{baseName}}=" + System.Uri.EscapeDataString(Str({{paramName}})));
{{/queryParams}}
            if (__q.Count > 0) path += "?" + string.Join("&", __q);
{{/hasQueryParams}}
            return _client.SendAsync("{{httpMethod}}", path, {{#bodyParam}}body: {{paramName}}, {{/bodyParam}}ct: ct);
        }
{{/returnType}}

{{/operation}}
        // Инвариантное строковое представление параметра для URL.
        private static string Str(object v)
        {
            if (v == null) return "";
            if (v is bool b) return b ? "true" : "false";
            if (v is System.IFormattable f)
                return f.ToString(null, System.Globalization.CultureInfo.InvariantCulture);
            return v.ToString();
        }
    }
{{/operations}}
{{#wrapApi}}
    }
{{/wrapApi}}
}
